<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="1024dp"
        android:layout_height="600dp"
        tools:context=".MainActivity"
        android:gravity="center">

        <kr.co.hdtel.htcommonui.view.HTActionBar
            android:id="@+id/tab_top"
            android:layout_width="match_parent"
            android:layout_height="62dp"
            android:background="#23a696"
            android:text="Face-Pass"
            android:textColor="@color/textcolor_wh"
            android:textSize="@dimen/actionbar_title_text_size"
            app:layout_constraintBottom_toTopOf="@+id/pager"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <kr.co.hdtel.facepass.SwipeViewPager
            android:id="@+id/pager"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:contentDescription="@string/app_name"
            android:background="#eeeeee"
            android:padding="16dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="0.0"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/tab_top" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>